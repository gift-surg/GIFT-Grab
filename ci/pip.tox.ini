[tox]
envlist = py27,py35
skipsdist = True

[testenv]
whitelist_externals =
    echo
passenv =
    GiftGrab_PIP_INSTALLER
    SCRATCH_DIR
    BOOST_ROOT
deps =
    pytest
    numpy
    scipy
commands =
    pip install {env:GiftGrab_PIP_INSTALLER}
